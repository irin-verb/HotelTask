
Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	Если МодульЗанятостиНомеров.НомерЗабронирован(Ссылка, НомерГостиницы, НачалоПериода, КонецПериода)
		ИЛИ МодульЗанятостиНомеров.НомерЗаселенБезПроверкиЗаселения(НомерГостиницы, НачалоПериода, КонецПериода) Тогда
		Сообщить("Номер нельзя забронировать на этот период!");
		Отказ = Истина;
	КонецЕсли;
КонецПроцедуры
